[metadata]
name = mqtt_ros2_bridge
version = 0.1.0

[options]
packages = find:
install_requires =
    rclpy
    std_msgs
    paho-mqtt
    PyYAML

[options.entry_points]
console_scripts =
    mqtt_ros2_bridge = mqtt_ros2_bridge.bridge_node:main
